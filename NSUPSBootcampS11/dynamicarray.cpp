#include <bits/stdc++.h>
#define Fast ios_base::sync_with_stdio(false), cin.tie(0);
using namespace std;
int main() {
        Fast;
        int n,x,y,t,z;
        int la = 0;
        cin>>n;
        cin>>q;
        vector<vector<int> > vect(n);
        vector <int> v;

        for(int i=0; i<q; i++) {
                cin>>t>>x>>y;
                vect.push_back(x)(y);
        }




}
