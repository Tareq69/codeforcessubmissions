#include<bits/stdc++.h>
using namespace std;

int main(){

    int n,p,q;
    cin>>n>>p;
    int arr1[p];
    int arr2[p];
    for(int i=0;i<p;i++){
        cin>>arr1[i];
    }
    cin>>q;
    for(int i=0;i<q;i++){
        cin>>arr2[i];
    }
    for(int i=0;i<p;i++){
      if(arr1[i]== ){
        cout<<"I become the guy."<<endl;
        exit(0);
      }
    }
cout<<"Oh, my keyboard!"<<endl;
}
